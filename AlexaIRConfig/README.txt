-------------- README ----------------
1. before executing the project you should have the IR code handy 
		(have to decode the IR remote values of the equipment that you are trying to control using Alexa)
2.Code is given in two separate folders

Serial_Arduino -> Folder contain the code that will intreract with NodeMCU in SPI mode and the Serial_Esp8266 give how Esp acts as mater in SPI to send the data to arduino when it receive signal from Alexa